# jira_author — the 4-level Jira plan (Initiative → Deliverable → Epic → Story)

> ⚠ **Stub (TASK-102).** The full skill lands at **TASK-122**. Role statement only; do not
> execute this stub against a run.

Authors `jira_plan.json` (§3.8, D11.6) from the accepted **SI v2** + `enrichment.json` +
`jira_template.<domain>.yaml` (D-A15): Initiative ← the document itself (§1/§2/§4);
Deliverable ← §7; Epic ← one per §8 requirement; **Story ← derived** from §16 entries (impacts
*and* gaps) and §7 non-code work — never read off the tech letter. Every story names its code
location or is flagged `new_build` / `non_code`; §16's granularity *is* story granularity. Runs
only after G2 — stories require enrichment evidence.
